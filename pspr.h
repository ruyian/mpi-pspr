#ifndef _MPI_SORT_H
#define _MPI_SORT_H

#include "mpi.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

#define INDEX(i, j, n) (((i)*(n))+(j))
#define SIZE 65535
#define RNG_MODIFIER 40

#endif